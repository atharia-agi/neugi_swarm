[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "neugi-swarm-v2"
version = "2.1.3"
description = "NEUGI Swarm V2 - Distributed AI Agent Orchestration Platform"
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.10"
authors = [
    {name = "NEUGI Team", email = "contact@neugi.ai"},
]
keywords = ["ai", "swarm", "agents", "orchestration", "distributed"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]

dependencies = [
    "requests>=2.31.0",
    "PyYAML>=6.0",
    "rich>=13.0",
    "cryptography>=41.0",
    "httpx>=0.25.0",
    "anyio>=4.0.0",
    "websockets>=11.0",
    "aiohttp>=3.9.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "pytest-asyncio>=0.23",
    "pytest-cov>=4.0",
    "ruff>=0.1.0",
    "mypy>=1.0",
]
full = [
    "sentence-transformers>=2.2.0",
    "scikit-learn>=1.3.0",
    "playwright>=1.40.0",
]

[project.scripts]
neugi = "neugi_swarm_v2.cli.cli:main"

[project.urls]
Homepage = "https://neugi.com"
Documentation = "https://atharia-agi.github.io/neugi_swarm/"
Repository = "https://github.com/atharia-agi/neugi_swarm"
Issues = "https://github.com/atharia-agi/neugi_swarm/issues"

[tool.setuptools.packages.find]
include = ["neugi_swarm_v2*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py", "*_test.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
addopts = "-v --tb=short --strict-markers"
markers = [
    "slow: marks tests as slow (deselect with '-m \"not slow\"')",
    "integration: marks tests as integration tests",
    "unit: marks tests as unit tests",
]

[tool.ruff]
target-version = "py310"
line-length = 100
src = ["neugi_swarm_v2"]

[tool.ruff.lint]
select = ["E", "F", "W", "I", "N", "UP", "B", "SIM"]
ignore = ["E501"]

[tool.mypy]
python_version = "3.10"
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true
